package main

import ( 
	"os"
	"fmt"
	"strconv"
)


func sum(x, y int) {
	c := x + y
	return c
}


func main() {
	
	arr := os.Args[1:]
	// i := arr[0]
	j := strconv.Atoi(os.Args[2])
	i, err := 
	j, err := strconv.Atoi(arr[1])
	*/

	fmt.Println("Hello world!")
	fmt.Println("The answer is ", j, err)
}